create database springbook;
use springbook;

create table springbook.usersusers(
                                      id varchar(10) primary key,
                                      name varchar(20) not null,
                                      password varchar(10) not null
)

select * from springbook.users;